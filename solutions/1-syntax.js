// BEGIN (write your solution here)
export const getWeekends = (format) => {
    if (format == 'short') {
        return ['sat', 'sun'];;
    } else {
        return ['saturday', 'sunday'];
    }
}
// END
